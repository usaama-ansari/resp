import { IncreamentPipe } from './increament.pipe';

describe('IncreamentPipe', () => {
  it('create an instance', () => {
    const pipe = new IncreamentPipe();
    expect(pipe).toBeTruthy();
  });
});
